# 启用自动记忆整理

## 使用场景

如果你希望系统在合适时机自动整理记忆、减少冗余并改善后续检索质量，需要先了解当前自动记忆整理行为。根据当前源码实现，这项能力当前默认返回关闭状态。

## 操作步骤

1. 先了解当前实现边界：虽然注释提到可通过 `enable_memory_organizer` 配置，但当前函数实现直接返回 `False`。
2. 这意味着就当前源码状态而言，它更像“尚未真正开放的能力说明”，而不是一个已经稳定可用的配置开关。
3. 如果后续版本将其真正接入配置，再按正式入口启用。

## 你会看到的提示与反馈

- 按当前实现，即使你尝试配置相关键名，也不应把文档写成“现在一定会生效”的承诺。
- 如果未来版本真正启用这项能力，才会出现自动整理记忆的实际行为变化。

## 注意事项

- 当前文档的重点是说明真实边界：源码注释与实际返回值存在差异。
- 不要把这项功能写成已经完全可操作，否则会误导用户。
