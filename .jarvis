提交代码: |
  1. 使用git add . 添加所有修改的文件
  2. 使用git diff --cached | cat - 查看修改的文件内容
  3. 根据修改的内容总结生成commit信息
  4. 使用git commit -m "commit信息" 提交修改
  5. 使用git push 提交到远程仓库
发布版本: 执行 python3 ./scripts/publish.py patch 进行版本发布
安装当前版本: 执行 python3 -m pip install -e . 安装当前python包
代码走查: 使用git工具，查看当前代码库修改的内容，进行code-review，如果有问题，给出修改意见
生成diff: 生成remote到HEAD的diff文件