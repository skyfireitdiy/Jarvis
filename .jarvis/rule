# 规则列表

## 部署相关

### 版本发布规则

用于规范Jarvis项目的版本发布流程，确保版本号的正确性、ReleaseNote的格式一致性，以及代码变更的准确性。（{{ git_root_dir }}/.jarvis/rules/deployment/version_release.md）

## 代码质量

### Jarvis编码规范

Jarvis AI IDE 代码开发指导规则，包含编码规范、工程结构规范、错误处理规范、数据处理规范、接口设计规范、性能与资源规范、依赖管理规范、类型注解规范等8个规范。（{{ git_root_dir }}/.jarvis/rules/code_quality/Jarvis编码规范.md）