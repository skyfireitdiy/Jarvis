---
name: Publish Docker Image to GHCR

'on':
  push:
    tags:
      - 'v*'

jobs:
  docker-publish:
    name: Build and publish Docker image to GHCR
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set registry token
        id: registry
        run: |
          if [ -n "${{ secrets.GHCR_ACCESS_TOKEN }}" ]; then
            echo "token=${{ secrets.GHCR_ACCESS_TOKEN }}" >> $GITHUB_OUTPUT
            echo "使用自定义 GHCR_ACCESS_TOKEN"
          else
            echo "token=${{ github.token }}" >> $GITHUB_OUTPUT
            echo "使用 GITHUB_TOKEN"
          fi

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/jarvis
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ steps.registry.outputs.token }}
          logout: false

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Make package public
        run: |
          # 使用与登录相同的 token
          TOKEN="${{ steps.registry.outputs.token }}"
          USERNAME="${{ github.repository_owner }}"
          echo "用户名: $USERNAME"
          
          # 尝试作为个人账户
          RESPONSE=$(curl -s -w "\n%{http_code}" -X PATCH \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $TOKEN" \
            https://api.github.com/user/packages/container/jarvis \
            -d '{"visibility":"public"}')
          
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          
          # 如果个人账户失败（404），尝试作为组织
          if [ "$HTTP_CODE" = "404" ]; then
            echo "尝试作为组织账户..."
            curl -X PATCH \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer $TOKEN" \
              https://api.github.com/orgs/$USERNAME/packages/container/jarvis \
              -d '{"visibility":"public"}'
          elif [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "204" ]; then
            echo "✅ 包已设置为公开"
          else
            echo "⚠️ 设置包为公开时返回状态码: $HTTP_CODE"
            echo "$RESPONSE" | head -n-1
          fi

