# -*- coding: utf-8 -*-
"""内置替换映射表模块。

该模块定义了Jarvis系统内置的默认替换映射表。
格式: {"标记名": {"template": "替换模板", "description": "描述信息"}}
"""

BUILTIN_REPLACE_MAP = {
    "sdd/1.spec": {
        "append": False,
        "template": """
请执行SDD规范阶段（Specification）- 需求澄清与规范编写：

## 你必须遵守的规则

- **必须**：没有Spec，禁止开始任何功能实现
- **必须**：Spec要清晰、具体、可验证，禁止模糊表述
- **必须**：遇到需求不明确时，先澄清再编写Spec
- **必须**：Spec与代码不一致时，先更新Spec
- **禁止**：跳过需求澄清直接编写Spec
- **禁止**：Spec中使用模糊表述（如"尽可能快"、"合理"）

## 你必须执行的操作

### 操作一：需求澄清

**执行步骤：**

1. 从模糊的想法出发，与用户/团队反复沟通以澄清需求
2. 自问："这个功能要解决什么问题？"而不是"这个功能如何实现？"
3. 从用户的角度思考功能需求，而不是从技术的角度
4. 明确功能的边界，哪些在内，哪些在外
5. 用用户故事的形式（作为...我需要...以便...）描述需求

### 操作二：识别边界与异常

**执行步骤：**

1. 考虑各种输入场景（正常、边界、异常）
2. 识别边界情况：空值、极值、特殊字符等
3. 识别异常情况：错误输入、网络故障、资源不足等
4. 为每种情况定义明确的处理方式

### 操作三：编写Spec文档

**执行步骤：**

1. Spec文档**必须**放在项目的 `.jarvis/spec/` 目录
2. Spec文档命名规范：
   - **必须**：使用小写字母
   - **必须**：使用下划线 `_` 分隔单词
   - **必须**：使用 `.md` 后缀
   - **禁止**：使用空格、连字符 `-` 或大写字母
   - ✅ 正确示例：`user_auth_spec.md`, `data_processing_spec.md`
   - ❌ 错误示例：`UserAuthSpec.md`, `user-auth-spec.md`

### 操作四：编写Spec必须包含的内容

**执行步骤：**

1. 功能概述：
   - 简要说明功能的目标与价值
   - 说明功能的使用场景
   - 说明功能要解决什么问题

2. 接口定义：
   - 函数/方法的签名（参数类型、返回类型）
   - API端点的定义（路径、方法、请求/响应格式）
   - 类的公开接口（属性与方法）

3. 输入输出说明：
   - 各参数的详细说明（类型、范围、约束）
   - 返回值的详细说明（类型、含义）
   - 可能抛出的异常或错误码

4. 功能行为：
   - 正常情况的处理（**必须**提供具体示例）
   - 边界情况的处理（空值、极值等）
   - 异常情况的策略（错误处理、降级方案）
   - 性能要求（如适用：响应时间、吞吐量等）

5. 验收标准：
   - 明确的通过条件（**必须**是可量化的标准）
   - 可验证的测试场景（正常、边界、异常）
   - 具体的验证方法（测试用例、检查清单）

### 操作五：Spec编写规范

**执行步骤：**

1. 使用清晰简洁的语言
2. **禁止**使用模糊表述（如"尽可能快"、"合理"），**必须**使用具体的数字与范围
3. **必须**提供具体示例以说明复杂需求
4. **必须**使用结构化形式（列表、表格、代码块）
5. **必须**为每个验收标准编号，便于追踪
6. 区分"必须"与"可选"的需求
7. 考虑向后兼容与可扩展性

### 操作六：Spec评审与确认

**执行步骤：**

1. 检查Spec的完整性：是否包含所有必需内容
2. 检查Spec的清晰度：是否易于理解与执行
3. 检查Spec的可验证性：验收标准是否明确
4. 与用户/团队确认Spec的内容
5. Spec**必须**经过评审与确认后才能进入下一阶段

## 产出物

- [ ] Spec文档已创建于 `.jarvis/spec/` 目录
- [ ] Spec文档命名合规（小写、下划线、.md后缀）
- [ ] Spec包含功能概述与目标
- [ ] Spec包含清晰的接口定义
- [ ] Spec包含完整的输入输出说明
- [ ] Spec包含功能行为（正常/边界/异常情况）
- [ ] Spec包含明确的验收标准
- [ ] Spec使用结构化形式，包含具体示例
- [ ] Spec已评审与确认
""",
        "description": "SDD规范阶段：编写功能规范",
    },
    "sdd/2.plan": {
        "append": False,
        "template": """
请执行SDD规划阶段（Planning）- 技术决策与实现规划：

## 你必须遵守的规则

- **必须**：所有技术决策都要有明确理由与记录
- **必须**：规划要与Spec一致，不得偏离
- **必须**：发现Spec的问题时先更新Spec再继续规划
- **必须**：规划要考虑可维护、可扩展、可测试的特性
- **禁止**：未完成规划就进入任务分解阶段
- **禁止**：规划偏离Spec所定义的功能边界

## 你必须执行的操作

### 操作一：规范一致性验证

**执行步骤：**

1. 仔细阅读Spec文档，理解所有需求
2. 分析规范的歧义：找出表述不清、可能产生多种理解的地方
3. 分析规范的矛盾：找出相互冲突的需求或约束
4. 分析规范的缺失：找出缺失的关键信息（如错误处理、性能要求等）
5. 如果发现歧义、矛盾或缺失，**必须**先更新Spec再继续规划

### 操作二：架构设计

**执行步骤：**

1. 确定整体架构方案：
   - 系统分层结构（表现层、业务层、数据层等）
   - 模块划分与职责定义
   - 模块间的依赖关系
   - 数据流与交互方式
2. 设计模块接口：
   - 定义模块的公开接口
   - 明确模块间的通信方式
   - 设计数据模型与数据结构

### 操作三：技术栈选择

**执行步骤：**

1. 为每项技术选择提供明确理由：
   - 为什么选择这个技术/框架/库？
   - 有哪些替代方案？为什么不采用？
   - 这个选择有哪些优缺点？
   - 是否符合项目约束（性能、安全、兼容性等）？
2. 记录技术决策文档，包含：
   - 决策的背景与问题描述
   - 考虑过的替代方案
   - 所选方案及理由
   - 风险评估与缓解策略

### 操作四：依赖分析

**执行步骤：**

1. 识别所有外部依赖：
   - 第三方库与框架
   - 系统服务与API
   - 数据库与存储
   - 网络与基础设施
2. 分析依赖关系：
   - 依赖的版本要求
   - 依赖间的兼容性
   - 依赖的许可要求
   - 依赖的维护状态

### 操作五：性能与安全规划

**执行步骤：**

1. 性能需求分析：
   - 响应时间要求（如：API响应<100ms）
   - 吞吐量要求（如：支持1000 QPS）
   - 资源使用限制（内存、CPU、存储）
   - 并发能力
2. 安全需求分析：
   - 认证与授权机制
   - 数据加密要求
   - 输入验证与防护
   - 安全审计与日志

### 操作六：实现路径规划

**执行步骤：**

1. 确定实现顺序：
   - 基础功能优先
   - 依赖关系优先
   - 高风险功能优先
2. 识别关键里程碑：
   - 每个里程碑的交付物
   - 里程碑间的依赖
   - 里程碑的验收标准

### 操作七：风险评估与缓解

**执行步骤：**

1. 识别潜在风险：
   - 技术风险（新技术、复杂实现）
   - 时间风险（工期紧张）
   - 资源风险（人力、硬件）
   - 依赖风险（第三方服务）
2. 制定风险缓解策略：
   - 替代方案
   - 降级策略
   - 监控与告警

## 产出物

- [ ] 已验证Spec的完整性与一致性，无歧义、矛盾或缺失
- [ ] 已确定架构方案，包含模块划分与依赖
- [ ] 已选择技术栈，每项选择都有明确理由的记录
- [ ] 已分析所有依赖，确认兼容性与版本要求
- [ ] 已明确性能要求与实现方案
- [ ] 已明确安全要求与实现方案
- [ ] 已规划实现路径与关键里程碑
- [ ] 已识别风险并制定缓解策略
- [ ] 规划文档已记录所有技术决策与理由
""",
        "description": "SDD规划阶段：技术决策与实现规划",
    },
    "sdd/3.task": {
        "append": False,
        "template": """
请执行SDD任务阶段（Tasks）- 任务分解与执行清单：

## 你必须遵守的规则

- **必须**：任务要具体、可行、可验证
- **必须**：每个任务都要有明确的验收标准
- **必须**：任务的依赖关系要清晰，避免循环依赖
- **必须**：任务的粒度要适中，不要过大也不要过细
- **禁止**：未完成任务分解就开始实现
- **禁止**：任务间出现循环依赖

## 你必须执行的操作

### 操作一：任务分解

**执行步骤：**

1. 基于Spec与规划，将整体功能分解为可行的任务
2. 任务粒度的要求：
   - 每个任务**必须**是独立、可测试的单元
   - 任务的大小适中（建议1-3天完成）
   - 任务的边界清晰，职责单一
3. 任务的类型：
   - 数据模型的任务：将领域概念转化为数据模型
   - API接口的任务：将用户故事转化为API接口定义
   - 业务逻辑的任务：实现核心业务功能
   - 测试的任务：将验证场景转化为测试用例
   - 文档的任务：编写用户文档与API文档

### 操作二：数据模型设计

**执行步骤：**

1. 识别所有实体与概念：
   - 从Spec提取领域概念
   - 定义实体的属性与关系
2. 设计数据模型：
   - 数据库表结构（如适用）
   - 数据类/结构定义
   - 数据验证规则
   - 数据迁移方案（如适用）

### 操作三：API接口设计

**执行步骤：**

1. 将用户故事转化为API接口：
   - 定义API端点（路径、方法）
   - 定义请求格式（参数、请求体）
   - 定义响应格式（成功响应、错误响应）
   - 定义状态码与错误码
2. 编写API文档：
   - 接口说明
   - 参数说明
   - 响应示例
   - 错误处理说明

### 操作四：测试用例设计

**执行步骤：**

1. 将验证场景转化为测试用例：
   - 正常场景测试用例
   - 边界情况测试用例
   - 异常场景测试用例
2. 测试用例的形式：
   - 测试名称与描述
   - 前置条件
   - 测试步骤
   - 预期结果
   - 测试数据

### 操作五：任务依赖分析

**执行步骤：**

1. 识别任务间的依赖：
   - 前置依赖：哪些任务应先完成
   - 后置依赖：哪些任务依赖当前任务
   - 并行任务：可以同时执行的任务
2. 构建任务依赖图：
   - 明确任务顺序
   - 识别关键路径
   - 优化任务调度

### 操作六：任务定义与验收标准

**执行步骤：**

1. 为每个任务定义：
   - 任务名称与描述
   - 任务的输入（依赖任务的输出、文档、数据）
   - 任务的输出（代码、文档、测试用例）
   - 验收标准（如何验证任务完成）
   - 预估工作量（时间、复杂度）
2. 验收标准的要求：
   - **必须**具体、可量化
   - **必须**可验证（通过测试或检查）
   - **必须**与Spec的验收标准相对应

### 操作七：任务优先级排序

**执行步骤：**

1. 根据依赖关系确定执行顺序
2. 根据风险与价值确定优先级：
   - 高风险任务优先
   - 核心功能优先
   - 阻塞其他任务的任务优先
3. 制定任务执行计划：
   - 任务执行顺序
   - 里程碑节点
   - 并行计划

## 产出物

- [ ] 已将功能分解为可行的任务清单
- [ ] 已设计数据模型，包含实体、属性与关系
- [ ] 已设计API接口，包含端点、请求/响应格式
- [ ] 已设计测试用例，覆盖正常、边界、异常场景
- [ ] 已明确各任务间的依赖关系
- [ ] 已为每个任务定义输入、输出与验收标准
- [ ] 已确定任务执行顺序与优先级
- [ ] 任务清单已确认，可以开始实现
""",
        "description": "SDD任务阶段：任务分解与执行清单",
    },
    "sdd/4.code": {
        "append": False,
        "template": """
请执行SDD实现阶段（Code Generation）- 基于规范编码：

## 你必须遵守的规则

- **禁止**：没有Spec就开始实现
- **禁止**：实现超出Spec范围的功能（除非先更新Spec）
- **禁止**：简化或省略Spec所要求的功能
- **禁止**：修改Spec所定义的接口签名
- **必须**：严格依照Spec执行，不得偏离
- **必须**：遇到Spec不明确时先更新Spec再继续
- **必须**：同时编写测试用例以验证验收标准
- **必须**：代码实现要与Spec一致

## 你必须执行的操作

### 操作一：准备工作

**执行步骤：**

1. 确认Spec文档已完整且经过评审
2. 确认规划文档已明确技术方案
3. 确认任务清单已完成分解
4. 准备开发环境：
   - 安装必要的工具与依赖
   - 配置开发环境
   - 设置代码规范与检查工具

### 操作二：依任务顺序执行

**执行步骤：**

1. 严格依照任务清单的顺序执行
2. 对每个任务：
   - 仔细阅读任务描述与验收标准
   - 参考Spec的相关部分
   - 参考规划中的技术方案
   - 实现代码功能
   - 同时编写测试用例

### 操作三：代码实现要求

**执行步骤：**

1. 严格依照Spec执行：
   - 接口定义**必须**与Spec一致
   - 功能行为**必须**符合Spec的描述
   - 输入输出**必须**符合Spec的要求
   - 异常处理**必须**符合Spec的定义
2. **禁止**偏离Spec的定义：
   - **禁止**实现超出Spec范围的功能（除非先更新Spec）
   - **禁止**简化或省略Spec所要求的功能
   - **禁止**修改Spec所定义的接口签名
3. 代码质量要求：
   - 遵循项目代码规范
   - 编写清晰的注释与文档
   - 使用有意义的变量与函数名
   - 保持代码结构清晰

### 操作四：测试驱动开发

**执行步骤：**

1. 同时编写测试用例：
   - 为每个功能编写单元测试
   - 测试正常场景、边界场景、异常场景
   - 测试覆盖率**必须**达到要求（建议>80%）
2. 测试用例要求：
   - 测试用例**必须**对应Spec的验收标准
   - 测试用例**必须**可重复执行
   - 测试用例**必须**有清晰的断言
3. 执行测试验证：
   - 实现后立即执行测试
   - 确认所有测试都通过
   - 修复测试失败的缺陷

### 操作五：处理Spec不明确的情况

**执行步骤：**

1. 如果实现中发现Spec不明确：
   - **必须**立即停止实现
   - 分析不明确之处
   - 更新Spec文档，澄清或补充信息
   - 重新评估更新后的Spec
   - 确认Spec完整后再继续实现
2. 如果发现Spec有错误：
   - **必须**立即停止实现
   - 记录错误与修复方案
   - 更新Spec文档
   - 重新评估更新后的Spec
   - 确认Spec正确后再继续实现

### 操作六：探索性验证

**执行步骤：**

1. 可以通过探索性实现验证规划的可行性：
   - 快速实现原型以验证想法
   - 验证技术方案的可行性
   - 发现潜在的问题与风险
2. 根据探索的结果：
   - 如果发现规划的问题，**必须**更新Spec
   - 如果发现技术的问题，**必须**更新规划
   - 如果发现任务的问题，**必须**更新任务清单

### 操作七：代码审查与自检

**执行步骤：**

1. 完成实现后自检：
   - 检查代码是否符合Spec
   - 检查代码是否合规
   - 检查测试是否通过
   - 检查代码质量
2. 准备代码审查：
   - 确认代码可编译/运行
   - 确认测试全部通过
   - 准备审查材料

## 产出物

- [ ] 已确认Spec、规划与任务清单完整
- [ ] 已依照任务顺序完成所有实现
- [ ] 代码实现与Spec完全一致
- [ ] 已同时编写测试用例，覆盖所有验收标准
- [ ] 所有测试用例已通过
- [ ] 遇到Spec不明确时已更新Spec
- [ ] 代码符合项目规范与良好实践
- [ ] 代码已通过自检，准备审查
""",
        "description": "SDD实现阶段：基于规范编码",
    },
    "sdd/5.verify": {
        "append": False,
        "template": """
请执行SDD验证阶段（Verification）- 验证符合规范：

## 你必须遵守的规则

- **必须**：所有验收标准都要验证通过
- **必须**：代码实现与Spec一致
- **必须**：文档与实现同步
- **必须**：测试用例要覆盖所有验收标准
- **必须**：验证失败要回到相应阶段修复
- **禁止**：省略任何验证步骤
- **禁止**：验证未通过就进入下一阶段

## 你必须执行的操作

### 操作一：功能验证

**执行步骤：**

1. 对照Spec验证实现的正确性：
   - 逐项检查Spec的功能要求
   - 验证功能是否全部实现
   - 验证功能行为是否符合Spec的描述
2. 验证接口定义：
   - 检查函数/方法签名是否与Spec一致
   - 检查API端点是否与Spec一致
   - 检查参数与返回类型是否匹配
3. 验证输入输出：
   - 检查参数验证是否符合Spec的要求
   - 检查返回值格式是否符合Spec的定义
   - 检查异常抛出是否符合Spec的定义

### 操作二：验收标准验证

**执行步骤：**

1. 确认所有验收标准都已满足：
   - 逐项检查Spec的验收标准
   - 执行对应的测试用例
   - 验证测试结果是否符合预期
2. 验证正常场景：
   - 用正常输入测试功能
   - 验证输出是否符合预期
   - 验证功能逻辑是否正确
3. 验证边界情况：
   - 测试空值、极值等边界情况
   - 验证边界处理是否符合Spec的要求
   - 验证边界情况的错误处理
4. 验证异常情况：
   - 测试各种异常输入
   - 验证异常处理是否符合Spec的要求
   - 验证错误信息与错误码是否正确

### 操作三：测试执行

**执行步骤：**

1. 执行所有测试用例：
   - 单元测试
   - 集成测试
   - 端到端测试（如适用）
2. 检查测试结果：
   - **必须**确认所有测试通过
   - 分析失败的测试用例
   - 修复测试发现的问题
3. 测试覆盖率检查：
   - 检查代码覆盖率是否达到要求
   - 识别未覆盖的代码路径
   - 补充缺失的测试用例

### 操作四：代码审查

**执行步骤：**

1. 代码质量审查：
   - 检查代码是否符合项目规范
   - 检查代码结构是否清晰
   - 检查代码注释是否完整
   - 检查变量与函数名是否合适
2. 代码一致性审查：
   - 检查代码实现与Spec的一致性
   - 检查代码实现与规划的一致性
   - 检查代码实现与任务描述的一致性
3. 代码安全审查：
   - 检查输入验证是否充分
   - 检查错误处理是否完善
   - 检查潜在的安全漏洞

### 操作五：文档检查

**执行步骤：**

1. 检查代码文档：
   - 函数/方法注释是否完整
   - 参数与返回值的说明是否清晰
   - 异常的说明是否完整
2. 检查API文档：
   - API文档是否与实现一致
   - API示例是否正确
   - 错误处理的说明是否完整
3. 检查用户文档：
   - 使用的说明是否清晰
   - 示例是否可行
   - 常见问题是否覆盖

### 操作六：性能验证（如适用）

**执行步骤：**

1. 如果Spec中有性能要求：
   - 执行性能测试
   - 验证响应时间是否符合要求
   - 验证吞吐量是否符合要求
   - 验证资源使用是否符合要求
2. 性能优化：
   - 如果性能不达标，执行优化
   - 更新Spec记录实际性能数据

### 操作七：问题处理与修复

**执行步骤：**

1. 如果验证发现问题：
   - 记录所有发现的问题
   - 分析问题的根本原因
   - 确定问题所属的阶段（实现/任务/规划/Spec）
2. 回到相应阶段修复：
   - 如果是实现的问题：回到实现阶段修复代码
   - 如果是任务的问题：更新任务清单，重新实现
   - 如果是规划的问题：更新规划文档，调整实现
   - 如果是Spec的问题：更新Spec文档，重新评估
3. 修复后重新验证：
   - 修复问题后**必须**重新执行验证
   - **必须**确认所有问题已解决
   - **必须**确认所有验收标准已满足

## 产出物

- [ ] 已对照Spec验证所有功能要求
- [ ] 已验证所有接口定义与Spec一致
- [ ] 已验证所有输入输出符合Spec的要求
- [ ] 已验证所有验收标准都已满足
- [ ] 已执行所有测试用例，全部通过
- [ ] 已检查代码覆盖率，达到要求
- [ ] 已完成代码审查，无质量问题
- [ ] 已检查文档，与实现一致
- [ ] 已执行性能验证（如适用），符合要求
- [ ] 已处理所有发现的问题
- [ ] 所有验证已完成，可以进入下一阶段
""",
        "description": "SDD验证阶段：验证符合规范",
    },
    "sdd/6.feedback": {
        "append": False,
        "template": """
请执行SDD反馈循环阶段（Feedback Loop）- 持续迭代优化：

## 你必须遵守的规则

- **必须**：根据产出的反馈更新规范
- **必须**：将运行中的问题转化为规范的约束
- **必须**：保持规范、实现与现状的一致
- **必须**：持续监控与优化
- **必须**：形成规范、实现、运行的闭环
- **禁止**：忽视产出环境的问题
- **禁止**：规范与实现脱节

## 你必须执行的操作

### 操作一：收集反馈信息

**执行步骤：**

1. 收集产出的指标：
   - 性能指标（响应时间、吞吐量、资源使用）
   - 错误率与异常情况
   - 使用情况与反馈
   - 系统监控数据
2. 收集故障信息：
   - 生产环境的故障与问题
   - 安全漏洞与安全隐患
   - 数据丢失或损坏的事件
   - 服务中断的事件
3. 收集用户反馈：
   - 功能使用情况
   - 用户体验问题
   - 功能改进建议
   - 新的需求

### 操作二：分析反馈信息

**执行步骤：**

1. 分析性能问题：
   - 识别性能瓶颈
   - 分析性能下降的原因
   - 评估性能优化的必要性
2. 分析安全问题：
   - 识别安全漏洞
   - 分析安全风险
   - 评估安全修复的优先级
3. 分析功能问题：
   - 识别功能缺失
   - 分析用户需求的变化
   - 评估功能改进的价值

### 操作三：更新规范

**执行步骤：**

1. 将生产指标转化为规范的约束：
   - 更新性能要求（如：响应时间要求更严格）
   - 更新资源使用的限制
   - 更新并发能力的要求
2. 将故障信息转化为规范的约束：
   - 添加新的错误处理要求
   - 添加新的安全要求
   - 添加新的容错与恢复要求
3. 将用户反馈转化为规范的更新：
   - 更新功能描述
   - 添加新的功能需求
   - 更新验收标准

### 操作四：更新实现

**执行步骤：**

1. 依照更新后的规范调整实现：
   - 优化性能瓶颈
   - 修复安全漏洞
   - 改进功能的实现
2. 实现新的约束：
   - 添加新的错误处理逻辑
   - 添加新的安全检查
   - 实现新的功能需求
3. 保持实现与规范的一致：
   - **必须**确认代码实现符合更新后的规范
   - **必须**确认接口定义与规范一致

### 操作五：更新测试与文档

**执行步骤：**

1. 更新测试用例：
   - 添加新的测试场景
   - 更新性能测试用例
   - 更新安全测试用例
   - 更新验收测试用例
2. 更新文档：
   - 更新API文档
   - 更新用户文档
   - 更新部署文档
   - 更新运维文档
3. 验证更新：
   - 执行更新后的测试用例
   - **必须**确认所有测试通过
   - 验证文档的准确性

### 操作六：持续监控与迭代

**执行步骤：**

1. 建立监控机制：
   - 设置性能监控指标
   - 设置错误告警
   - 设置安全监控
2. 定期回顾：
   - 定期回顾生产指标
   - 定期分析用户反馈
   - 定期评估规范的有效性
3. 持续优化：
   - 根据监控数据持续优化
   - 根据用户反馈持续改进
   - 形成规范、实现与现状的持续迭代

## 产出物

- [ ] 已收集生产指标与故障信息
- [ ] 已分析反馈信息，识别问题与改进点
- [ ] 已更新规范，反映新的约束与要求
- [ ] 已更新实现，符合更新后的规范
- [ ] 已更新测试用例，覆盖新的场景
- [ ] 已更新文档，保持与实现一致
- [ ] 已建立监控机制，持续跟踪
- [ ] 已形成持续迭代的流程
""",
        "description": "SDD反馈循环阶段：持续迭代优化",
    },
    "GenerateRule": {
        "append": False,
        "template": """
请根据当前任务执行中积累的经验，生成一条可复用的Rule（规则）文档。

📝 Rule生成的详细步骤：

第一步：回顾任务执行过程
1. 分析当前任务的核心目标与最终结果
2. 回顾执行中所遇到的问题与解决方案
3. 识别成功的经验与失败的教训
4. 提取可复用的良好实践

第二步：确定规则的类型与位置

1. 确定规则适用的范围：
   - 全局规则：所有项目通用的规则
     * 存放位置：`{{ jarvis_data_dir }}/rules/<category>/<rule_name>.md`
     * 典型场景：通用编码规则、安全标准、性能优化策略等
   - 项目规则：仅适用于当前项目的特定规则
     * 存放位置：`{{ git_root_dir }}/.jarvis/rules/<category>/<rule_name>.md`
     * 典型场景：项目特定的架构规则、开发流程、具体配置等

2. 选择规则的类型（参考以下类别）：
   - architecture_design（架构设计）：设计模式、架构原则、代码组织结构
   - code_quality（代码质量）：代码规范、审查标准、文档要求
   - deployment（部署规范）：部署流程、发布规范、环境配置
   - development_tools（开发工具）：开发工具使用、脚本生成、工具配置
   - development_workflow（开发流程）：开发方法、工作流程、协作流程
   - performance（性能优化）：性能分析、优化策略、资源管理
   - security（安全规范）：安全编码、漏洞分析、安全测试
   - tool_config（工具配置）：工具配置文件规范、规范管理
   - ui_design（UI 设计）：界面设计规范、主题配置

3. 文件名规范：
   - 必须：使用小写字母
   - 必须：使用下划线 `_` 分隔单词
   - 必须：使用 `.md` 后缀
   - 禁止：使用空格、连字符 `-` 或大写字母
   - 示例：`code_review.md`, `tdd.md`, `security.md`

第三步：编写规则文档内容

规则文档必须包含以下结构：

1. YAML 头部（必须）：
```yaml
---
name: 规则名 # 必须，规则文档名（不含.md后缀）
description: 规则功能描述 # 必须，详细的触发场景说明
license: MIT # 可选
---
```

2. description 字段格式要求：
```
当需要[触发条件]时触发。每当用户提及"[关键词1]"、"[关键词2]"时触发。不触发：[条件1]；[条件2]；[条件3]。
```
说明：description只包含触发条件、触发词、不触发条件，禁止写功能介绍/技术说明/规则内容/操作列表。

3. 规则正文结构：
```markdown
# 规则标题

## 规则介绍

简要说明规则的用途、适用场景与预期效果。

## 你必须遵守的规则

### 1. 规则名称

**要求的明确：**

- **必须**：具体要求1
- **必须**：具体要求2
- **禁止**：禁止的事项

**示例：**

```python
# 示例代码
def example():
    pass
```

## 你必须执行的操作

### 操作一：操作名称

**执行步骤：**

1. 步骤1
2. 步骤2
3. 步骤3

**注意事项：**

- 注意事项1
- 注意事项2

## 检查清单

完成任务后，你必须确认：

- [ ] 检查项1
- [ ] 检查项2

## 相关资源

- 参考文档：`related_rule.md`
```

4. Jinja2 变量使用：
   - `{{ rule_file_dir }}`：当前规则文档所在目录
   - `{{ git_root_dir }}`：Git 仓库的根目录（由 git rev-parse 求得，取"当前工作目录所在仓库"的根；在仓库外运行时回退为当前工作目录）
   - `{{ current_dir }}`：当前工作目录
   - `{{ jarvis_src_dir }}`：Jarvis 源代码目录（由源码位置上溯求得，与运行目录无关，是 Jarvis 自身资源唯一稳定的定位变量）
   - `{{ jarvis_data_dir }}`：Jarvis 数据目录
   - 必须：尽量使用 jinja2 变量替代绝对路径
   - 禁止：在规则文档中硬编码绝对路径
   - 禁止：用 `{{ git_root_dir }}` 指向 Jarvis 自身资源（browser_extension/、docs/、builtin/rules/、ReleaseNote.md、.jarvis/ 等）——
     用户常在仓库外运行，此时 git_root_dir 会退化为当前目录，这类路径全部失效；指向 Jarvis 自身资源一律用 `{{ jarvis_src_dir }}`。
     `{{ git_root_dir }}` 只用于指向用户自己项目内的位置（如 `{{ git_root_dir }}/.jarvis/rules/...`、`{{ git_root_dir }}/project_info/...`）。
   - 必须：用 `{{ jarvis_src_dir }}` 时区分仓库根下资源（直接拼，如 `{{ jarvis_src_dir }}/browser_extension/...`）
     与 Python 包内源码（须含 src/jarvis，如 `{{ jarvis_src_dir }}/src/jarvis/jarvis_tools/...`）。

第四步：保存规则文档

1. 使用 write_file 工具创建规则文档到正确的位置
2. 确认文档内容符合上述格式要求
3. 验证文档已正确创建

输出格式示例：

规则文档的路径：`{{ git_root_dir }}/.jarvis/rules/<category>/<rule_name>.md`

```markdown
---
name: rule_name
description: 当需要[触发条件]时触发。每当用户提及"[关键词1]"、"[关键词2]"时触发。不触发：[条件1]；[条件2]；[条件3]。
---

# 规则标题

## 规则介绍

[介绍内容]

## 你必须遵守的规则

### 1. 规则名称

**要求的明确：**

- **必须**：[要求]
- **禁止**：[禁止]

## 你必须执行的操作

### 操作一：[操作名称]

**执行步骤：**

1. [步骤]

## 检查清单

- [ ] [检查项]
```

要点原则：
- 必须：Rule应当基于实际经验，禁止凭空编造
- 必须：Rule应当具体可行，避免模糊描述
- 必须：Rule应当有通用性，能应用于相似场景
- 必须：包含完整的 YAML 头部与 description 字段
- 必须：使用正确的文档路径与命名规范
- 禁止：生成过于具体或仅适用于当前任务的Rule
- 禁止：生成与现有Rule重复的内容
- 禁止：在规则中硬编码绝对路径
""",
        "description": "根据当前经验生成Rule",
    },
    "GenerateMethodology": {
        "append": False,
        "template": """
请根据当前任务执行中积累的经验，生成一条可复用的方法论（Methodology）。

📝 方法论生成的详细步骤：

第一步：回顾任务执行过程
1. 分析当前任务的核心目标与最终结果
2. 回顾执行中所遇到的问题与解决方案
3. 识别成功的经验与失败的教训
4. 提取可复用的问题解决方法与决策流程

第二步：确定方法论的类型

方法论与 Rule 的区别：
- **Rule**：编码规范、良好实践、具体操作指南（如何做）
- **Methodology**：问题解决方法、工作流程、决策框架（如何思考）

方法论适用的场景：
- 部署开源项目
- 生成提交信息
- 代码重构流程
- 故障排查流程
- 性能优化策略
- 架构设计的决策
- 技术选型的方法
- 问题分析的框架

第三步：确定方法论存放的位置

1. 全局方法论：所有项目通用的方法
   - 存放位置：`{{ jarvis_data_dir }}/methodologies/`
   - 典型场景：通用部署流程、代码审查方法、故障排查框架等

2. 项目级方法论：仅适用于当前项目的特定方法
   - 存放位置：`{{ git_root_dir }}/.jarvis/methodologies/`
   - 典型场景：项目特定的构建流程、测试策略、发布规范等

第四步：编写方法论的内容

方法论必须包含以下结构内容：

1. **问题类型（problem_type）**：
   - 简明扼要，能概括方法论解决的核心问题
   - 使用英文或拼音，如：`deploy_open_source_project`
   - 用于系统的检索与匹配

2. **方法论的内容（content）**：
   - **场景说明**：什么情况下需要使用此方法
   - **核心原则**：指导决策的基本原则（3-5条）
   - **执行步骤**：详细的步骤流程（按顺序编号）
   - **决策框架**：关键决策点的判断标准
   - **检查清单**：完成后的验收项
   - **注意事项**：常见的陷阱与避免的方法

第五步：保存方法论

使用 methodology 工具保存方法论，格式如下：

```
操作：add
问题类型：[problem_type]
适用范围：global 或 project
内容：[详细的结构化方法论内容]
```

输出格式示例：

```json
{
  "problem_type": "deploy_open_source_project",
  "content": "# 部署开源项目方法论\n\n## 场景说明\n\n当需要部署开源项目到生产环境时使用此方法。\n\n## 核心原则\n\n1. **安全优先**：确保部署过程不引入安全漏洞\n2. **可回滚**：任何部署都应当支持快速回滚\n3. **最小权限**：使用最小必要的权限执行任务\n\n## 执行步骤\n\n### 第一步：环境准备\n\n1. 检查系统依赖\n2. 配置运行环境\n3. 设置网络与防火墙\n\n### 第二步：项目部署\n\n1. 克隆项目代码\n2. 安装依赖\n3. 配置环境变量\n4. 启动服务\n\n### 第三步：验证与监控\n\n1. 验证服务状态\n2. 配置监控告警\n3. 设置日志收集\n\n## 决策框架\n\n- 如果项目有 Docker 支持，优先容器化部署\n- 如果项目需要数据库，先部署数据库再部署应用\n- 如果为生产环境，必须配置备份与恢复策略\n\n## 检查清单\n\n- [ ] 服务正常启动\n- [ ] 端口可访问\n- [ ] 日志无报错\n- [ ] 监控已配置\n\n## 注意事项\n\n- 避免使用 root 权限运行服务\n- 确保防火墙只开放必要的端口\n- 定期更新依赖与安全补丁"
}
```

要点原则：
- 必须：方法论应当基于实际经验，禁止凭空编造
- 必须：方法论应当有通用性，能应用于相似场景
- 必须：方法论应当包含清晰的决策框架与执行步骤
- 必须：使用 methodology 工具保存，确保格式正确
- 禁止：生成过于具体或仅适用于当前任务的方法
- 禁止：生成与现有方法重复的内容
- 禁止：将编码规范类的内容写入方法（应使用 Rule）
""",
        "description": "根据当前经验生成方法论",
    },
    "AutoSelectRule": {
        "append": False,
        "template": """
当前的规则已不足以支持任务，请使用 auto_select_rule 工具获取更多规则以扩展能力。

📝 使用步骤：

1. 分析当前任务的核心需求
2. 调用 auto_select_rule 工具，提供清晰的任务描述
3. 获取最相关的规则（至多5个）
4. 将获取的规则应用于任务的执行中
""",
        "description": "当规则不足时，使用auto_select_rule获取更多规则",
    },
}
