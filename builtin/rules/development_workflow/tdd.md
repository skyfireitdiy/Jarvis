---
name: tdd
description: 当需要先编写测试再实现功能的TDD开发流程时触发。每当用户提及"测试驱动"、"TDD"、"红绿重构"、"先写测试"、"测试先行"时触发。不触发：仅编写测试不涉及TDD流程；仅运行已有测试；调试测试失败但不采用TDD方式。
---

# 测试驱动开发 (TDD) 规则

## 汝必遵循之工作流程

### 红-绿-重构循环（严格执行）

1. **🔴 红阶段（编写失败之测试）**：
   - 在实现任何功能代码之前，必先编写测试用例
   - 测试必描述期望之行为，而不是实现细节
   - 运行测试，确认测试失败（红色）

2. **🟢 绿阶段（让测试通过）**：
   - 编写最简单之代码让测试通过
   - 不要过度设计，只实现让测试通过之最小代码
   - 运行测试，确认测试通过（绿色）

3. **🔵 重构阶段（优化代码）**：
   - 在测试通过之基础上，优化代码结构
   - 重构后必再次运行测试，确保全测试仍然通过
   - 若重构导致测试失败，立即回滚或修复

## 汝必遵守之原则

### 测试优先原则

- **禁**：在编写测试之前实现功能代码
- **必**：为每新功能编写测试用例
- **必**：测试应该清晰、可读、可维护
- **必**：用描述性之测试名称，清楚表达测试意图

### 测试覆盖要求

- **必**：为全新功能编写对应之测试
- **必**：测试覆盖以下三种情况：
  - 正常情况（happy path）
  - 边界情况（edge cases）
  - 异常情况（error cases）
- **必**：确保测试独立运行，不依赖其他测试
- **必**：确保测试可重复执行，结果一致

## 实践指导

### 编写测试时之思考方式

- 问自己："这个功能应该做什么？"而不是"这个功能如何实现？"
- 测试应该验证行为，而不是实现细节
- 一个测试只验证一个行为或场景

### 发现 Bug 时之处理流程

1. 先编写测试用例重现该 Bug
2. 运行测试，确认测试失败（重现问题）
3. 修复代码
4. 运行测试，确认测试通过（问题已解决）

## 代码示例

```python
# ✅ 正确做法：先写测试
def test_calculate_total():
    assert calculate_total([1, 2, 3]) == 6
    assert calculate_total([]) == 0
    assert calculate_total([-1, 1]) == 0

# 然后实现功能
def calculate_total(numbers):
    return sum(numbers)
```

## 执行检查清单

在提交代码前，汝必确认：

- [ ] 全新功能都有对应之测试用例
- [ ] 测试覆盖了正常、边界和异常情况
- [ ] 全测试都能独立运行
- [ ] 全测试都能通过
- [ ] 测试名称清晰描述测试意图
